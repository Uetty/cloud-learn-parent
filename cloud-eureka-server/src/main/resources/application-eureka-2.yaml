spring:
  application:
    name: eurekaServer-2

server:
  port: 8762

eureka:
  instance:
    hostname: server2
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:8761,http://localhost:8763

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF